# --- Core API runtime ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29
pydantic>=2.5
pydantic-settings>=2.0
python-dotenv>=1.0

# --- LLM / retrieval ---
openai>=1.30
supabase>=2.4
numpy>=1.26,<3.0

# If (and only if) you truly use Google *gRPC* clients right now, UNCOMMENT this:
# grpcio-status>=1.62.2
# grpcio>=1.62.2

# If you wire Google Drive later (REST, not strictly gRPC):
google-api-python-client>=2.129
google-auth>=2.29
google-auth-oauthlib>=1.2
pandas>=2.2
